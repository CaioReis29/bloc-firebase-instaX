name: user_repository
description: Dart package witch manages the user


publish_to: 'none' 

version: 1.0.0+1

environment:
  sdk: '>=3.0.6 <4.0.0'

dependencies:
  flutter:
    sdk: flutter

  equatable: ^2.0.5
 
  # firebase
  
dev_dependencies:
  flutter_test:
    sdk: flutter
 
  
  flutter_lints: ^2.0.0

flutter:
 
  uses-material-design: true